{
  "name": "redis-development-build",
  "version": "1.0.0",
  "description": "Build tooling for Redis Development skill",
  "type": "module",
  "scripts": {
    "build": "npx tsx src/build.ts --all",
    "eval": "npx tsx src/eval-anthropic-matrix.ts",
    "eval:aggregate": "npx tsx src/eval-aggregate.ts",
    "eval:baseline": "npx tsx src/eval-baseline.ts",
    "validate": "npx tsx src/validate.ts",
    "dev": "npm run validate && npm run build"
  },
  "keywords": [
    "redis",
    "performance",
    "guidelines",
    "llm",
    "agents"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
