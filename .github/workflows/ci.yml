name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false

      - name: Install skill-validator
        run: |
          version="$(node -p "require('./package.json').skillValidatorVersion")"
          go install "github.com/agent-ecosystem/skill-validator/cmd/skill-validator@${version}"

      - name: Install dependencies
        run: npm ci

      - name: Validate
        run: npm run validate
